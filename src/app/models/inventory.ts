export interface Inventory {
  pdi_code: string;
  ctg_code: string;
  item: string;
  ctg: string;
  descrpt: string;
  supplier: string;
  unit: string;
  location: string;
  qty: number;
  price: number;
}
