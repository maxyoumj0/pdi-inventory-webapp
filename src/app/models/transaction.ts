export interface Transaction {
  transId: string;
  supplier_code: string;
  pdi_code: string;
  descrpt: string;
  employee: string;
  mvt: string;
  qty: number;
  unit: string;
  location: string;
  date: string;
  rest: string;
}
